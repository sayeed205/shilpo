// Source SHA-256: 23ad406b0ee62947692655b2ced88404731b720a3b08ffeefad1d992be98e298
// @generated by sdk/typescript/scripts/generate.ts
// DO NOT EDIT DIRECTLY. Source: core/ext-api/wit/extension.wit
// Package: shilpo:extension@0.1.0

/** Opaque string identifier for an extension (e.g., "org.shilpo.weather"). */
export interface ExtensionId {
  value: string;
}

/** Opaque string identifier for a single contribution within an extension. */
export interface ContributionId {
  value: string;
}

/** Canonical combined identity ("extension_id/contribution_id"). */
export interface CanonicalId {
  extensionId: string;
  contributionId: string;
}

/** Request correlation identifier for asynchronous host import operations. */
export type RequestId = string;

/** Monotonic activation correlation identifier. */
export type ActivationId = string;

/** Monotonic deadline represented as remaining milliseconds. */
export interface Deadline {
  remainingMs: bigint;
}

/** Classification of typed errors returned by host imports. */
export type ErrorKind =
  | "unauthorized"
  | "invalid-argument"
  | "not-found"
  | "rate-limited"
  | "unsupported"
  | "internal"
  | "backend-unavailable"
  | "locked"
  | "denied"
  | "cancelled";

/** Standard error payload returned by host imports. */
export interface ErrorPayload {
  kind: ErrorKind;
  message: string;
}

/** Opaque reference handle to an encrypted secret stored in host secret service. */
export interface SecretRef {
  handle: string;
}

/**
 * Typed scalar data value used for extension settings, input, state, and
 * action payloads across the WIT boundary.
 */
export type DataValue =
  | { tag: "none" }
  | { tag: "bool-value"; val: boolean }
  | { tag: "int-value"; val: bigint }
  | { tag: "float-value"; val: number }
  | { tag: "text-value"; val: string }
  | { tag: "bytes-value"; val: Uint8Array }
  | { tag: "secret-ref"; val: SecretRef };

export interface ActivationInput {
  contributionId?: string;
  eventId?: string;
  value?: DataValue;
}

export interface Caller {
  id: string;
  kind: string;
}

/** Explicit trigger origin for component activation. */
export type ActivationOrigin =
  | "shell-startup"
  | "contribution-mount"
  | "user-input"
  | "event-dispatch"
  | "system-resume";

/** Enclosing context provided during component activation/start. */
export interface Activation {
  id: ActivationId;
  origin: ActivationOrigin;
  extensionId: string;
  contributionId?: string;
  input?: ActivationInput;
  caller?: Caller;
  deadline?: Deadline;
}

/** Reason for orderly guest component deactivation. */
export type DeactivateReason =
  | "user-requested"
  | "shell-shutdown"
  | "reload"
  | "error-quarantine";

/** Scope classification of search modes supported by search providers. */
export type SearchMode =
  | "default"
  | "apps"
  | "actions"
  | "clipboard"
  | "calculator"
  | "command"
  | "web-search"
  | "keybindings";

/** Classification of candidate search results. */
export type SearchResultCategory =
  | "window"
  | "application"
  | "action"
  | "clipboard"
  | "calculator"
  | "command"
  | "web-search"
  | "file-path"
  | "uri"
  | "keybinding"
  | "custom";

/** Icon descriptor for search candidate presentation. */
export type SearchIcon =
  | { tag: "none" }
  | { tag: "named"; val: string }
  | { tag: "asset"; val: string };

/** Inbound query request passed to an extension search provider. */
export interface SearchRequest {
  rawQuery: string;
  query: string;
  mode: SearchMode;
  generation: bigint;
}

/** Search candidate emitted by an extension search provider. */
export interface SearchCandidate {
  id: string;
  title: string;
  subtitle?: string;
  aliases: string[];
  keywords: string[];
  category: SearchResultCategory;
  icon?: SearchIcon;
  activationVerb: string;
  activationPayload: string;
}

// --- Wallpaper Interface ---

export type WallpaperMode = "manual" | "slideshow";

export type WallpaperTargetKind = "global" | "workspace";

export type WallpaperRequestReason =
  | "activate"
  | "user-next"
  | "slideshow-tick"
  | "workspace-changed"
  | "settings-changed";

export interface WorkspaceTarget {
  workspaceId: string;
  outputName?: string;
}

export type WallpaperTarget =
  | { tag: "global" }
  | { tag: "workspace"; val: WorkspaceTarget };

export interface WallpaperRequest {
  requestId: RequestId;
  contributionId: string;
  reason: WallpaperRequestReason;
  mode: WallpaperMode;
  target: WallpaperTarget;
}

export type WallpaperSource = "extension-asset" | "local-file" | "remote";

export interface WallpaperInfo {
  path: string;
}

export interface WallpaperSetRequest {
  requestId?: RequestId;
  path: string;
  source: WallpaperSource;
  target?: WallpaperTarget;
}

// --- Events Interface ---

export type EventKind =
  | "outputs-changed"
  | "theme-changed"
  | "palette-generated"
  | "wallpaper-changed"
  | "network-changed"
  | "media-changed"
  | "power-changed"
  | "timer-fired"
  | "workspace-changed";

export type BarMenuCloseReason =
  | "source-toggle"
  | "escape"
  | "focus-lost"
  | "outside-click"
  | "overview-opened"
  | "bar-closed"
  | "display-removed"
  | "owner-removed"
  | "source-unavailable";

export interface ThemeEvent {
  mode: string;
}

export interface PaletteEvent {
  accent: string;
}

export interface WallpaperEvent {
  path: string;
}

export interface NetworkEvent {
  connected: boolean;
}

export interface MediaEvent {
  title?: string;
  artist?: string;
  playing: boolean;
}

export interface PowerEvent {
  percentage?: number;
  charging: boolean;
}

export interface TimerEvent {
  name: string;
}

export interface WorkspaceEvent {
  workspaceId: string;
  workspaceName?: string;
  outputName?: string;
}

export interface WallpaperResultEvent {
  requestId: RequestId;
  success: boolean;
  error?: string;
}

export interface MountedEvent {
  contributionId: string;
  instanceId?: string;
  width: number;
  height: number;
}

export interface UnmountedEvent {
  contributionId: string;
  instanceId?: string;
}

export interface ResizedEvent {
  contributionId: string;
  instanceId?: string;
  width: number;
  height: number;
}

export interface SettingsEvent {
  contributionId: string;
  instanceId?: string;
  settings: DataValue;
}

export interface BarMenuOpenedPayload {
  contributionId: string;
  instanceId: string;
}

export interface BarMenuClosedPayload {
  contributionId: string;
  instanceId: string;
  reason: BarMenuCloseReason;
}

export interface InputEvent {
  contributionId: string;
  instanceId?: string;
  eventId: string;
  value?: DataValue;
}

export interface StateEvent {
  watchId: bigint;
  key: string;
  value?: DataValue;
  revision: bigint;
}

export interface HttpResponseEvent {
  requestId: string;
  status?: number;
  body: string;
  error?: string;
}

export interface LocationResponseEvent {
  latitude?: number;
  longitude?: number;
  accuracyMeters?: number;
  error?: string;
}

export type ExtensionEvent =
  | { tag: "shell-started" }
  | { tag: "shell-stopping" }
  | { tag: "outputs-changed" }
  | { tag: "theme-changed"; val: ThemeEvent }
  | { tag: "palette-generated"; val: PaletteEvent }
  | { tag: "wallpaper-changed"; val: WallpaperEvent }
  | { tag: "network-changed"; val: NetworkEvent }
  | { tag: "media-changed"; val: MediaEvent }
  | { tag: "power-changed"; val: PowerEvent }
  | { tag: "timer-fired"; val: TimerEvent }
  | { tag: "workspace-changed"; val: WorkspaceEvent }
  | { tag: "wallpaper-request"; val: WallpaperRequest }
  | { tag: "wallpaper-result"; val: WallpaperResultEvent }
  | { tag: "contribution-mounted"; val: MountedEvent }
  | { tag: "contribution-unmounted"; val: UnmountedEvent }
  | { tag: "contribution-resized"; val: ResizedEvent }
  | { tag: "contribution-settings-changed"; val: SettingsEvent }
  | { tag: "bar-menu-opened"; val: BarMenuOpenedPayload }
  | { tag: "bar-menu-closed"; val: BarMenuClosedPayload }
  | { tag: "input"; val: InputEvent }
  | { tag: "state-value"; val: StateEvent }
  | { tag: "http-response"; val: HttpResponseEvent }
  | { tag: "location-response"; val: LocationResponseEvent };

// --- View Interface ---

export type ContainerDirection =
  | { tag: "row" }
  | { tag: "column" }
  | { tag: "stack" }
  | { tag: "grid"; val: number };

export type Alignment = "start" | "center" | "end" | "stretch";

export type Justification =
  | "start"
  | "center"
  | "end"
  | "space-between"
  | "space-around";

export type Overflow = "visible" | "hidden" | "scroll";

export type SemanticColorToken =
  | "primary"
  | "on-primary"
  | "secondary"
  | "surface"
  | "surface-container"
  | "on-surface"
  | "on-surface-variant"
  | "outline"
  | "error";

export interface ViewStyle {
  padding?: number;
  margin?: number;
  width?: number;
  height?: number;
  cornerRadius?: number;
  opacity?: number;
  color?: SemanticColorToken;
  background?: SemanticColorToken;
  flexGrow?: number;
  borderWidth?: number;
  borderColor?: SemanticColorToken;
  minWidth?: number;
  maxWidth?: number;
  minHeight?: number;
  maxHeight?: number;
  overflow?: Overflow;
}

export interface ContainerNode {
  direction: ContainerDirection;
  children: Uint32Array;
  style?: ViewStyle;
  gap?: number;
  alignItems?: Alignment;
  justifyContent?: Justification;
  wrap: boolean;
  eventId?: string;
}

export interface TextNode {
  content: string;
  fontSize?: number;
  bold?: boolean;
  style?: ViewStyle;
}

export interface IconNode {
  name: string;
  size?: number;
  style?: ViewStyle;
}

export interface ImageNode {
  assetPath: string;
  width?: number;
  height?: number;
  style?: ViewStyle;
}

export interface ButtonNode {
  label: string;
  eventId: string;
  style?: ViewStyle;
}

export interface IconButtonNode {
  iconName: string;
  eventId: string;
  style?: ViewStyle;
}

export interface ToggleNode {
  value: boolean;
  eventId: string;
  style?: ViewStyle;
}

export interface SliderNode {
  value: number;
  min: number;
  max: number;
  eventId: string;
  style?: ViewStyle;
}

export interface TextInputNode {
  placeholder?: string;
  value: string;
  eventId: string;
  style?: ViewStyle;
}

export interface ListNode {
  items: Uint32Array;
  style?: ViewStyle;
}

export interface SpacerNode {
  size?: number;
}

export interface BadgeNode {
  label: string;
  style?: ViewStyle;
}

export interface ProgressNode {
  value: number;
  style?: ViewStyle;
}

export interface LoadingIndicatorNode {
  size?: number;
  color?: SemanticColorToken;
  style?: ViewStyle;
}

export type ViewNode =
  | { tag: "container"; val: ContainerNode }
  | { tag: "text"; val: TextNode }
  | { tag: "icon"; val: IconNode }
  | { tag: "image"; val: ImageNode }
  | { tag: "button"; val: ButtonNode }
  | { tag: "icon-button"; val: IconButtonNode }
  | { tag: "toggle"; val: ToggleNode }
  | { tag: "slider"; val: SliderNode }
  | { tag: "text-input"; val: TextInputNode }
  | { tag: "list"; val: ListNode }
  | { tag: "spacer"; val: SpacerNode }
  | { tag: "divider" }
  | { tag: "badge"; val: BadgeNode }
  | { tag: "progress"; val: ProgressNode }
  | { tag: "loading-indicator"; val: LoadingIndicatorNode };

export interface ViewTree {
  nodes: ViewNode[];
  root: number;
}

// --- Host Import Interfaces ---

export interface ActionsHost {
  invoke(actionId: string, payload?: DataValue): Result<void, ErrorPayload>;
}

export interface ClipboardHost {
  read(): Result<string, ErrorPayload>;
  write(text: string): Result<void, ErrorPayload>;
}

export interface FilesystemHost {
  readFile(path: string): Result<Uint8Array, ErrorPayload>;
  writeFile(path: string, contents: Uint8Array): Result<void, ErrorPayload>;
}

export interface HttpRequest {
  requestId: RequestId;
  url: string;
  method: string;
  headers: Array<[string, string]>;
  body?: Uint8Array;
  deadline?: Deadline;
}

export interface HttpHost {
  request(req: HttpRequest): Result<RequestId, ErrorPayload>;
  cancel(reqId: RequestId): Result<void, ErrorPayload>;
}

export interface LocationHost {
  read(): Result<RequestId, ErrorPayload>;
}

export interface NotificationRequest {
  title: string;
  body: string;
  icon?: string;
}

export interface NotificationsHost {
  show(req: NotificationRequest): Result<void, ErrorPayload>;
}

export interface StateSnapshot {
  value?: DataValue;
  revision: bigint;
}

export interface StateMutation {
  changed: boolean;
  revision: bigint;
}

export interface WatchRegistration {
  watchId: bigint;
  snapshot: StateSnapshot;
}

export interface StateHost {
  read(key: string): Result<StateSnapshot, ErrorPayload>;
  write(key: string, value: DataValue): Result<StateMutation, ErrorPayload>;
  delete(key: string): Result<StateMutation, ErrorPayload>;
  watch(key: string): Result<WatchRegistration, ErrorPayload>;
  unwatch(watchId: bigint): Result<void, ErrorPayload>;
}

export interface SecretsHost {
  set(purpose: string, value: Uint8Array): Result<SecretRef, ErrorPayload>;
  read(purpose: string, reference: SecretRef): Result<Uint8Array | undefined, ErrorPayload>;
  delete(purpose: string, reference: SecretRef): Result<void, ErrorPayload>;
}

export interface ThemeInfo {
  mode: string;
  accent: string;
}

export interface ThemeHost {
  read(): Result<ThemeInfo, ErrorPayload>;
  setSourceColor(color: string): Result<void, ErrorPayload>;
}

export interface WallpaperHost {
  read(): Result<WallpaperInfo, ErrorPayload>;
  set(req: WallpaperSetRequest): Result<void, ErrorPayload>;
}

// --- Result Helper ---

export type Result<T, E = ErrorPayload> =
  | { ok: true; value: T }
  | { ok: false; error: E };

export function ok<T>(value: T): Result<T, never> {
  return { ok: true, value };
}

export function err<E>(error: E): Result<never, E> {
  return { ok: false, error };
}
